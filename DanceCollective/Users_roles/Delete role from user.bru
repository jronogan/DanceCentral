meta {
  name: Delete role from user
  type: http
  seq: 2
}

delete {
  url: {{server}}/users-roles/roles
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access}}
}

body:json {
  {
    "user_id": 1,
    "role_name": "choreographer"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
