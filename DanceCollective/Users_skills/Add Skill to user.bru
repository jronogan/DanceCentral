meta {
  name: Add Skill to user
  type: http
  seq: 1
}

post {
  url: {{server}}/users-skills/skills
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access}}
}

body:json {
  {
    "user_id": 1,
    "skill_name": "waacking"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
